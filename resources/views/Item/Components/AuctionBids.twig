<script type="x/template" id="vue-auction-bids">
    
    {#<h3>${ auction.expiryDate }</h3>#}
    <auction-countdown
            template="#vue-auction-countdown"
            :deadline="deadline"
    >
    </auction-countdown>
    
    
    <div class="row container" id="auction-bidform">
        
        <div class="col-lg-8 offset-lg-3" formGroup="maxBidForm">
            <input class="form-control form-control-lg text-muted"
                    v-model="maxCustomerBid" number
                    min="${ minbid }"
                    type="number"
                    id="maximumBid"
                    placeholder="{{ trans("PluginAuctions::Template.maxBid") }}"
                    aria-describedby="maxBidHelpBlock"
            >
            <p id="maxBidHelpBlock"
                    class="form-text text-muted text-center">{{ trans("PluginAuctions::Template.bidAtLeast") }}<span
                        :class="{alarm: !isInputValid}">${ minbid | currency }</span>{{ trans("PluginAuctions::Template.bidAtLeastEnd") }}
            </p>
            
            
{#
            <div v-resource-if:user="isLoggedIn">
                <a href="#"
                        class="dropdown-toggle"
                        id="accountMenuList"
                        data-toggle="dropdown"
                        aria-haspopup="true"
                        aria-expanded="false">
                    <i class="fa fa-user hidden-sm-up" aria-hidden="true"></i>
                    <span class="hidden-xs-down">{{ trans("Ceres::Template.generalHello", {"username": "${username}"}) }}</span></a>
                </a>
                <div class="account-menu dropdown-menu dropdown-menu-right small">
                    <div class="list-group" aria-labelledby="accountMenuList">
                        <a href="/my-account"
                                class="list-group-item small"><i class="fa fa-user"></i> {{ trans("Ceres::Template.accMyAccount") }}
                        </a>
                        <a href="#"
                                class="list-group-item small"
                                v-logout><i class="fa fa-sign-out"></i> {{ trans("Ceres::Template.accLogout") }}</a>
                    </div>
                </div>
            </div>
#}
            
            <a href="#bid-confirm-modal" class="dropdown-toggle btn btn-primary btn-lg btn-block"
                    id="test"
                    data-toggle="dropdown"
                    aria-haspopup="true"
                    aria-expanded="false">
                <i class="fa fa-user hidden-sm-up" aria-hidden="true"></i>
                <span class="hidden-xs-down">{{ trans("PluginAuctions::Template.makeBindingBid") }}</span></a>
            </a>
            
            <button class="btn btn-primary btn-lg btn-block"
                    type="submit"
                    :disabled="!isInputValid"
                    @click="addBid"
            >{{ trans("PluginAuctions::Template.makeBindingBid") }}</button>
            
            {#<button class="btn btn-primary btn-lg btn-block"#}
            {#v-if="userdata.id == 7076"#}
            {#@click="auctionend"#}
            {#>auctionend#}
            {#</button>#}
            
            {#ToDo: agb abfrage...#}
        </div>
    </div>

</script>
{#<style>#}
    {#.alarm {#}
        {#color: #c50000;#}
    {#}#}

{#</style>#}