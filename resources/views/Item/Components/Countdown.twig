<script type="x/template" id="vue-auction-countdown">
    <div class="row container">
        <dl class="auctionList m-y-2">
            <dt>Restzeit:</dt>
            <dd>
                <span>
                <ul class="vue-countdown">
                    <li v-show="(days > 0)">
                        <p class="digit">${ days }</p>
                        <p class="text">tage</p>
                    </li>
                    <li>
                        <p class="digit" :class="{alarm: (days < 1) }">${ hours }</p>
                        <p class="text">std.</p>
                    </li>
                    <li>
                        <p class="digit" :class="{alarm: (days < 1) }">${ minutes }</p>
                        <p class="text">min</p>
                    </li>
                    <li v-if="(days < 1)">
                        <p class="digit" :class="{alarm: (days < 1) }">${ seconds }</p>
                        <p class="text">sec</p>
                    </li>
                </ul>
                </span>
            </dd>
        </dl>
    </div>
</script>

<dl class="auctionList m-y-1">
    <dt>Start Datum:</dt>
    <dd>{{ auction.startDate | date('D d. M Y H:i') }}</dd>


    <style>
        .vue-countdown {
            padding: 0;
            margin: 0;
            margin-top: -12px;
        }

        .vue-countdown li {
            display: inline-block;
            margin: 0 8px;
            text-align: center;
            position: relative;
        }

        .vue-countdown li p {
            margin: 0;
        }

        .vue-countdown li:after {
            content: ":";
            position: absolute;
            top: 0;
            right: -13px;
            font-size: 32px;
        }

        .vue-countdown li:first-of-type {
            margin-left: 0;
        }

        .vue-countdown li:last-of-type {
            margin-right: 0;
        }

        .vue-countdown li:last-of-type:after {
            content: "";
        }

        .vue-countdown .digit {
            font-size: 32px;
            font-weight: 600;
            line-height: 1.4;
            margin-bottom: 0;
        }

        .vue-countdown .alarm {
            color: #c50000
        }

        .vue-countdown .text {
            text-transform: uppercase;
            margin-bottom: 0;
            font-size: 10px;
        }
    </style>
