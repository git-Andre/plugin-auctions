<script type="x/template" id="vue-auction-show-bidderlist">

    <div class="row container">

        <div class="row container m-b-1">
            <div class="col-md-6 p-t-5">
                <h4>
                    ${bidders}<span class="small muted">{{ trans("PluginAuctions::auctions.bidderListBidders") }}</span>${bidderList.length -
                    1}<span class="small muted">{{ trans("PluginAuctions::auctions.bidderListBids") }}</span>
                </h4>
            </div>

        </div>

        <p class="muted small p-l-1">{{ trans("PluginAuctions::auctions.bidderListMaxBids1") }}<br>{{ trans("PluginAuctions::auctions.bidderListMaxBids2") }}</p>

        <div class="tab-pane" id="ao-bidderList" role="tabpanel">

            <div class="m-y-2">
                <table class="table table-striped table-hover table-sm">
                    <tbody>
                    <tr>
                        <th class="column-left p-l-1"><h4><span class="p-r-1"><i class="fa fa-user fa-2"
                                            aria-hidden="true"></i></span>{{ trans("PluginAuctions::auctions.bidderListHighestBidder") }}
                            </h4></th>
                        <th class="column text-right p-r-3"><h4><span class="p-r-1"><i class="fa fa-money fa-1"
                                            aria-hidden="true"></i></span>{{ trans("PluginAuctions::auctions.bidderListHighestBid") }}</h4></th>
                        <th class="column-right text-right p-r-1"><h4><span class="p-r-1"><i class="fa fa-clock-o"
                                            aria-hidden="true"></i></span>{{ trans("PluginAuctions::auctions.bidderListBidTime") }}</h4></th>
                    </tr>
                    <tr v-for="bid in bidderList">
                        <td class="bid p-l-3">${ bid.bidderName }</td>
                        <td class="bid text-right p-r-1">
                            <span class="p-r-1 small text-muted">${ bid.bidStatus }</span>
                            ${ bid.bidPrice | currency }
                        </td>
                        <td class="bid text-right p-r-2">${ bid.bidTimeStamp | date ("ddd DD.MMM.YYYY HH:mm:ss") }
                        </td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</script>


<style>
    .bid {
        font-family: 'Abel', sans-serif;
    }

    .column-right {
        width: 30%;
    }

    .column-left {
        width: 26%;
    }

    .column {
        width: 40%;
    }
</style>